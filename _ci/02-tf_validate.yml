terraform:validate:
  stage: validate
  script:
    - terraform init
    - terraform workspace select $WORKSPACE
    - terraform validate
  only:
    refs: 
      - merge_requests
  tags:
    - dind
  parallel: !reference [.parallel,parallel]